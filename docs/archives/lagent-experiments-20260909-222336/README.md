# LAgent 历史实验归档与暂停点

归档时间：2026-09-09 22:23:36（Asia/Shanghai）。
用户要求：归档现有工作，然后暂停任务。后续开发等待用户通知恢复。

## 已验证的交付范围

- LE-001/002：实验规格、永久记录、产物与幂等登记的离线契约。
- LE-003～011：历史包/时点查询、阶段与模拟账户、保守执行、候选隔离、
  费用/标定、Episode/共享队列、评分/比较/复用、基线 CAS 与留出冻结的内部实现。
  其中真实历史数据、正式模型/搜索/成本/资源能力及完整集成仍未验收。
- LE-012：83 个 API operations，32 个历史实验命令，CLI 与 skill 同步。
  包含草案/预检、候选、历史包、定义、计划、取消/关联重跑、比较、
  标定、选择/冻结、审计读取/下载，以及进化树/节点测试/比较/选择历史。
- LE-013：进化树查询基础已验证；父版本、比较对手和历史基线分别保留，
  双序号分页不倒填后续状态，复用按原 Test 去重，隐藏明细继续宿主审计。

完整进度见 [implementation-progress.md](../../tickets/lagent-experiments/implementation-progress.md)；
全部范围及状态见 [tickets 索引](../../tickets/lagent-experiments/README.md)；
规则依据见 [设计定稿](../../research/lagent-experiment-final-design.md)。

## 最后已完成的验证

- clean-shell self-test：152 Node + 1911 Python 通过，1 条既有 Starlette 弃用提示。
- 运行开始 2026-09-09T14:09:05.432Z，完成 14:13:34.461Z；
  原报告已复制到本目录 self-test.json，ok=true、exitCode=0。
- CLI maintenance：83 operations / 32 个实验命令；个人 skill 链接、命令参考与文档检查通过。
- 该轮 API 已重载，验证时 PID 60878；六个健康/目录入口为 200。
  四个新增查询对未知实验为 404，越界 limit 为 422，安装 CLI 同样透传。
- 验证时真实库没有实验草案；正向案例使用隔离数据库及合成合同，
  没有运行真实模型或历史 Episode，没有正式准入或实际交易。

## 暂停时尚未验收的前端草稿

以下改动已留在工作区，并随代码快照归档：
- frontend/src/research/ExperimentExplorer.tsx（新增）
- frontend/src/research/experimentContracts.ts（新增）
- frontend/src/research/ResearchConfigurationPage.tsx（接入浏览组件）

草稿包含实验目录、候选列表、节点测试/比较、选择历史、预检/配置读取与显式
审计入口。TypeScript 无输出类型检查已通过；尚未完成 CSS、前端交互测试、
构建、浏览器视觉检查或针对这批前端改动的完整 self-test。
上面的 1911 项验证发生在前端草稿之前，不覆盖这批改动。

恢复时应先核对并完善该草稿：
- 运行事件时间字段应使用宿主 actual_at，目前草稿误读 created_at。
- 以用户可读的结构化摘要展示记录/阶段/成本；原 JSON 只作辅助证据。
- 完成样式和窄屏/键盘/列表交互，验证分页缓存与旧响应隔离。
- 验证审计只由明确点击发起、失败重试沿用原身份、隐藏内容不自动加载。
- 补齐前端测试、构建和浏览器验证，再按仓库要求运行完整检查。
当前没有启动浏览器预览或留下测试/构建进程；没有为归档继续实现功能。

## 剩余首版工作

LE-013 前端与完整 Episode 可观测下钻；LE-012 正式执行提交；
真实历史/规则数据、日期受控搜索、模型 usage/硬费用与完整资源计量；
真实合格评估生产者及共享服务完整接线；LE-014 故障/反作弊全流程验收；
LE-015 实际预检、标定和原例研究。LE-016 自动外层 optimizer 属于后续阶段。
目标尚未完成，也没有以 fixture 或入口已存在代替正式验收。

## 归档内容与恢复原则

source-snapshot.tar.gz 保存当前实验及共享研究代码、相关数据库/API/CLI、
前端源码、相关测试、设计/tickets 和 ahunter skill。
source-manifest.json 逐文件记录路径、字节数和 SHA-256；
archive-integrity.json 记录快照及清单校验值。

这是当前工作区的源码检查点，包含共享模块的既有上下文与未验收前端草稿；
不代表其中所有既有改动均由本次开发产生，不是整机或可直接运行的数据备份。
未归档真实数据库、日志、运行产物、凭据、个人运行配置、虚拟环境或 node_modules。
未提交/重置 Git，也未覆盖或清理其他工作区改动。
恢复时先在临时目录解包、核对清单、与当前工作区比较，避免直接覆盖后续用户修改。
