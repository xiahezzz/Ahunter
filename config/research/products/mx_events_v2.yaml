product: mx_events@2
title: 按关注对象分组的本地 MX 资讯
providers: [local-mx]
result_schema: {type: object, required: [status, feeds]}
required_fields: [status, feeds]
feed_scope:
  kind: mx_rid_feeds
  required: [rids]
  rids:
    type: integer
    minimum: 1
    maximum: 9007199254740991
    min_items: 1
    max_items: 100
retention_class: information
