# A Hunter CLI command reference

Generated by `python -m advisor.cli.maintenance --write`; do not edit by hand.

All commands start with `ahunter`. Global options: `--base-url`, `--timeout`, `--pretty` (accepted before or after subcommands).
`ahunter commands` returns the full machine-readable command and field catalog without contacting the API.

Body flags can be replaced with `--json FILE` or `--json -` (stdin). JSON uses wire field names below, not option names. Do not mix JSON input and body flags.
Required path values are positional, in the order shown. Query defaults and bounds are enforced by the API. Repeated filters use repeated flags.

## `research experiments presets original-case`

Read the editable original-case draft; this does not resolve data or enable execution.

`GET /api/research/experiments/presets/original-case` · Reads state

## `research experiments create`

Store an immutable draft; incomplete configuration is retained with validation errors.

`POST /api/research/experiments` · Writes state

Requires `--json FILE` (or `--json -`), containing a JSON object.

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| JSON only | `config` | object | Required | — |
| JSON only | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |

## `research experiments list`

List experiment draft identities.

`GET /api/research/experiments` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--offset` | `offset` | int | Optional | Zero-based offset |

## `research experiments show <experiment_id>`

Read original configuration and validation errors; draft status is not execution status.

`GET /api/research/experiments/{experiment_id}` · Reads state

## `research experiments preflight <experiment_id>`

Append configuration/local-inventory readiness evidence; HTTP success can contain blocked.

`POST /api/research/experiments/{experiment_id}/preflight` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--submission-identity` | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |

## `research experiments preflights list <experiment_id>`

List immutable preflight results.

`GET /api/research/experiments/{experiment_id}/preflights` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--offset` | `offset` | int | Optional | Zero-based offset |

## `research experiments bundles import <experiment_id>`

Import an explicitly selected local historical bundle; validation is not formal source acceptance.

`POST /api/research/experiments/{experiment_id}/bundles/import` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--root` | `root` | str | Required | Absolute local directory containing manifest.json and declared source/data files |
| `--submission-identity` | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |

## `research experiments definitions resolve <experiment_id>`

Resolve complete config against sealed calendar bytes at initial research time; retain blocked attempts.

`POST /api/research/experiments/{experiment_id}/definitions/resolve` · Writes state

Requires `--json FILE` (or `--json -`), containing a JSON object.

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| JSON only | `config` | object | Required | — |
| JSON only | `calendar_bundle_id` | str | Required | — |
| JSON only | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |

## `research experiments plans register <experiment_id>`

Atomically register the complete predeclared sample matrix; Tests remain created and are not queued.

`POST /api/research/experiments/{experiment_id}/plans` · Writes state

Requires `--json FILE` (or `--json -`), containing a JSON object.

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| JSON only | `definition_id` | str | Required | — |
| JSON only | `plan` | object | Required | — |
| JSON only | `purpose` | str | Required | — |
| JSON only | `selection_id` | str | Required | Explicit null except the frozen final-holdout selection ID |
| JSON only | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |

## `research experiments selection show <experiment_id>`

Read current initial/selected baseline IDs and freeze state without hidden samples or exposure reports.

`GET /api/research/experiments/{experiment_id}/selection` · Reads state

## `research experiments selection initialize <experiment_id>`

Fix the root baseline before any Test starts; one canonical initialization identity per experiment.

`POST /api/research/experiments/{experiment_id}/selection/initialize` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--definition-id` | `definition_id` | str | Required | — |
| `--baseline-id` | `baseline_id` | str | Required | Original candidate_proposal record ID, not its local proposal name |

## `research experiments selection apply <experiment_id>`

Commit a completed comparison decision against its original selection revision; eligibility alone never moves the baseline.

`POST /api/research/experiments/{experiment_id}/selection/apply` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--comparison-id` | `comparison_id` | str | Required | — |
| `--expected-selection-id` | `expected_selection_id` | str | Required | — |
| `--submission-identity` | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |

## `research experiments selection finalize-holdout <experiment_id>`

Permanently freeze selection after terminal Tests, known cleanup and unseen holdout checks; does not run holdout.

`POST /api/research/experiments/{experiment_id}/selection/finalize-holdout` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--expected-selection-id` | `expected_selection_id` | str | Required | — |
| `--submission-identity` | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |

## `research experiments candidates register <experiment_id>`

Seal uploaded original bytes and register a distinct proposal; never executes code or creates Tests.

`POST /api/research/experiments/{experiment_id}/candidates` · Writes state

Requires `--json FILE` (or `--json -`), containing a JSON object.

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| JSON only | `manifest` | object | Required | CandidateInput: declared relative file paths, entrypoint, contracts and allowed research config |
| JSON only | `files_base64` | object | Required | Exact declared path to canonical base64 bytes map; API never reads host paths |
| JSON only | `proposal` | object | Required | proposal_id, parent_proposal_id (nullable), hypothesis, source |
| JSON only | `diff_base64` | str | Required | Canonical base64 diff bytes or explicit null |
| JSON only | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |

## `research experiments tree <experiment_id>`

Page proposal lineage, original Test status counts and baseline markers at fixed record/event ceilings.

`GET /api/research/experiments/{experiment_id}/tree` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--cursor` | `cursor` | str | Optional | Opaque cursor from the previous page; keep filters unchanged |

## `research experiments candidates tests <experiment_id> <proposal_id>`

Page every original and linked rerun Test for a proposal; outcomes and traces remain audited.

`GET /api/research/experiments/{experiment_id}/candidates/{proposal_id}/tests` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--cursor` | `cursor` | str | Optional | Opaque cursor from the previous page; keep filters unchanged |

## `research experiments candidates comparisons <experiment_id> <proposal_id>`

Page original comparison opponents and permitted aggregate feedback separately from proposal parentage.

`GET /api/research/experiments/{experiment_id}/candidates/{proposal_id}/comparisons` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--cursor` | `cursor` | str | Optional | Opaque cursor from the previous page; keep filters unchanged |

## `research experiments selection history <experiment_id>`

Page original selection decisions and baseline changes without hidden exposure reports or samples.

`GET /api/research/experiments/{experiment_id}/selection/history` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--cursor` | `cursor` | str | Optional | Opaque cursor from the previous page; keep filters unchanged |

## `research experiments candidates list <experiment_id>`

List typed original proposal metadata with a stable cursor; no Test outcomes.

`GET /api/research/experiments/{experiment_id}/candidates` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--cursor` | `cursor` | str | Optional | Opaque cursor from the previous page; keep filters unchanged |

## `research experiments candidates show <experiment_id> <proposal_id>`

Read a local proposal ID, verified package manifest and original parent links; no execution permission.

`GET /api/research/experiments/{experiment_id}/candidates/{proposal_id}` · Reads state

## `research experiments records list <experiment_id>`

List record metadata and Test status without exposing hidden detail.

`GET /api/research/experiments/{experiment_id}/records` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--kind` | `kind` | str | Optional | — |
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--cursor` | `cursor` | str | Optional | Opaque cursor from the previous page; keep filters unchanged |

## `research experiments records show <experiment_id> <record_id>`

Read an original record with durable hidden-scope exposure auditing.

`POST /api/research/experiments/{experiment_id}/records/{record_id}/detail` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--audit-identity` | `audit_identity` | str | Required | Stable identity for this exact read; hidden detail commits owner exposure before release |

## `research experiments tests events <experiment_id> <test_id>`

Read original attempts, phase/account/cost observations and lifecycle events; hidden reads are audited.

`POST /api/research/experiments/{experiment_id}/tests/{test_id}/events` · Writes state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--after` | `after` | int | Optional | — |
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--audit-identity` | `audit_identity` | str | Required | Stable identity for this exact read; hidden detail commits owner exposure before release |

## `research experiments tests cancel <experiment_id> <test_id>`

Cancel an unstarted Test or durably request shared-service cleanup; 202 remains pending.

`POST /api/research/experiments/{experiment_id}/tests/{test_id}/cancel` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--submission-identity` | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |
| `--reason` | `reason` | str | Required | — |

## `research experiments tests rerun <experiment_id> <test_id>`

Register a new linked Test from a terminal original; remains created and cannot replace original comparison samples.

`POST /api/research/experiments/{experiment_id}/tests/{test_id}/rerun` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--submission-identity` | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |
| `--reason` | `reason` | str | Required | — |

## `research experiments records export <experiment_id> <record_id>`

Download an audited record manifest with original links, artifact hashes and retention availability.

`POST /api/research/experiments/{experiment_id}/records/{record_id}/export` · Writes state

Requires `--output PATH`; writes original bytes to a new file and returns path, byte count, SHA-256, and content type. Existing files are never replaced.

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--audit-identity` | `audit_identity` | str | Required | Stable identity for this exact read; hidden detail commits owner exposure before release |

## `research experiments records artifact <experiment_id> <record_id> <artifact_hash>`

Download linked original bytes with audit and retention checks.

`POST /api/research/experiments/{experiment_id}/records/{record_id}/artifacts/{artifact_hash}` · Writes state

Requires `--output PATH`; writes original bytes to a new file and returns path, byte count, SHA-256, and content type. Existing files are never replaced.

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--audit-identity` | `audit_identity` | str | Required | Stable identity for this exact read; hidden detail commits owner exposure before release |

## `research experiments comparisons feedback <experiment_id> <record_id>`

Read permitted aggregate selection feedback; final-holdout and unknown scopes are rejected.

`GET /api/research/experiments/{experiment_id}/comparisons/{record_id}/feedback` · Reads state

## `research experiments comparisons freeze <experiment_id>`

Freeze the original sample pair and declared conditions before execution; uploaded evidence does not confer runtime acceptance.

`POST /api/research/experiments/{experiment_id}/comparisons` · Writes state

Requires `--json FILE` (or `--json -`), containing a JSON object.

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| JSON only | `plan_id` | str | Required | — |
| JSON only | `baseline` | str | Required | — |
| JSON only | `candidate` | str | Required | — |
| JSON only | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |
| JSON only | `previous_comparison` | str | Required | Prior inconclusive result ID for a new plan, or explicit null |
| JSON only | `expected_selection_id` | str | Required | — |
| JSON only | `runtime_conditions` | object | Required | Exact task-to-condition mapping, or explicit null for an unqualified comparison |
| JSON only | `runtime_artifacts_base64` | object | Required | Exact referenced hash-to-canonical-base64 original bytes map; empty when conditions are null |

## `research experiments comparisons complete <experiment_id> <record_id>`

Derive one immutable result after all original Tests are terminal; returns aggregate feedback, not audited sample detail.

`POST /api/research/experiments/{experiment_id}/comparisons/{record_id}/complete` · Writes state

## `research experiments calibrations freeze <experiment_id>`

Freeze original baseline calibration conditions before execution; one canonical campaign per definition.

`POST /api/research/experiments/{experiment_id}/calibrations` · Writes state

Requires `--json FILE` (or `--json -`), containing a JSON object.

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| JSON only | `plan_id` | str | Required | — |
| JSON only | `expected_selection_id` | str | Required | — |
| JSON only | `table` | object | Required | Versioned comparison-equivalent USD PriceTable declaration |
| JSON only | `envelopes` | array | Required | ResourceEnvelope for every predeclared task |
| JSON only | `artifacts_base64` | object | Required | Every referenced evidence hash and its exact canonical-base64 original bytes |

## `research experiments calibrations complete <experiment_id> <record_id>`

Derive immutable allocations from all original completed and metered calibration repeats; detail requires audited record reads.

`POST /api/research/experiments/{experiment_id}/calibrations/{record_id}/complete` · Writes state

## `research lagent settings get`

Read versioned LAgent configuration.

`GET /api/research/lagent/settings` · Reads state

## `research lagent settings update`

Publish complete LAgent configuration with optimistic concurrency.

`PUT /api/research/lagent/settings` · Writes state

Requires `--json FILE` (or `--json -`), containing a JSON object.

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| JSON only | `expected_version` | int | Required | — |
| JSON only | `config` | object | Required | — |

## `research lagent requests create`

Queue free LAgent research with one main investigator and dynamic subagents.

`POST /api/research/lagent/requests` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--scope` | `scope` | str | Required | security or market; must match the selected Team |
| `--code` | `code` | str | Optional | — |
| `--task` | `task` | str | Required | — |
| `--submission-identity` | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |
| `--expected-version` | `expected_version` | int | Required | — |

## `research lagent requests trace <request_id>`

Read pinned configuration, task tree and paginated invocation/data/query events.

`GET /api/research/lagent/requests/{request_id}/trace` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--after` | `after` | int | Optional | — |
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |

## `research lagent requests artifact <request_id> <artifact_hash>`

Download an integrity-checked input or result linked to this request's trace.

`GET /api/research/lagent/requests/{request_id}/artifacts/{artifact_hash}` · Reads state

Requires `--output PATH`; writes original bytes to a new file and returns path, byte count, SHA-256, and content type. Existing files are never replaced.

## `health`

Read collector health.

`GET /api/health` · Reads state

## `current-state`

Read dashboard state and quality.

`GET /api/current-state` · Reads state

## `services status`

Read service status without starting services.

`GET /api/services` · Reads state

## `mx listener status`

Read passive Listener status.

`GET /api/mx/listener/status` · Reads state

## `mx listener start`

Request Listener start; never starts Chrome.

`POST /api/mx/listener/start` · Writes state

## `mx listener stop`

Request Listener stop.

`POST /api/mx/listener/stop` · Writes state

## `mx chrome start`

Explicitly start the fixed dedicated Chrome; no login or navigation.

`POST /api/mx/chrome/start` · Writes state

## `mx rids get`

Read authorized RIDs and configuration version.

`GET /api/mx/rids` · Reads state

## `mx rids replace`

Replace the full RID set with user-supplied values; JSON rids: [] explicitly clears it.

`PUT /api/mx/rids` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--version` | `version` | str | Required | Current version from mx rids get |
| `--rid` | `rids` | array | Required / repeat flag | Authorized RID; repeat for every RID to retain |

## `mx events list`

Search and paginate accepted MX events.

`GET /api/mx/events` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--cursor` | `cursor` | str | Optional | Opaque cursor from the previous page; keep filters unchanged |
| `--rid` | `rid` | array | Optional / repeat flag | — |
| `--authorization` | `authorization` | str | Optional | — |
| `--start-at` | `start_at` | str | Optional | Epoch milliseconds or ISO datetime |
| `--end-at` | `end_at` | str | Optional | Epoch milliseconds or ISO datetime |
| `--has-media` | `has_media` | str | Optional | true or false |
| `--q` | `q` | str | Optional | Search text |

## `mx events get <event_id>`

Read one stored event.

`GET /api/mx/events/{event_id}` · Reads state

## `mx media download <event_id> <media_id>`

Save original media bytes to a new file.

`GET /api/mx/events/{event_id}/media/{media_id}` · Reads state

Requires `--output PATH`; writes original bytes to a new file and returns path, byte count, SHA-256, and content type. Existing files are never replaced.

## `market-daily status`

Read Market Daily status.

`GET /api/market-daily/status` · Reads state

## `market-daily cold-start`

Queue the idempotent five-year cold start for execution after 21:00.

`POST /api/market-daily/cold-start` · Writes state

## `market-daily runs list`

List market ingestion runs.

`GET /api/market-daily/runs` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |

## `market-daily runs get <run_id>`

Read one market ingestion run.

`GET /api/market-daily/runs/{run_id}` · Reads state

## `market-daily runs failures <run_id>`

Read paginated source failures.

`GET /api/market-daily/runs/{run_id}/failures` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--offset` | `offset` | int | Optional | Zero-based offset |

## `reports list`

List verified report archives.

`GET /api/reports` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--cursor` | `cursor` | str | Optional | Opaque cursor from the previous page; keep filters unchanged |
| `--start-date` | `start_date` | str | Optional | — |
| `--end-date` | `end_date` | str | Optional | — |

## `reports get <report_date> <report_type>`

Read a verified premarket or review archive.

`GET /api/reports/{report_date}/{report_type}` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--run-id` | `run_id` | str | Optional | Defaults to initial; use the run_id from the listing |

## `research cycles list`

List legacy cycle artifacts.

`GET /api/research/cycles` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |

## `research cycles get <report_date> <cycle_id>`

Read a legacy cycle artifact.

`GET /api/research/cycles/{report_date}/{cycle_id}` · Reads state

## `research execution-settings get`

Read execution policy and available models.

`GET /api/research/execution-settings` · Reads state

## `research execution-settings update`

Update model settings using the current policy reference.

`PUT /api/research/execution-settings` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--expected-policy-ref` | `expected_policy_ref` | str | Required | — |
| `--model` | `model` | str | Required | — |
| `--reasoning-effort` | `reasoning_effort` | str | Required | — |

## `research agents list`

List published Research Agents.

`GET /api/research/agents` · Reads state

## `research data-catalog`

List published Data Products.

`GET /api/research/data-catalog` · Reads state

## `research agent-access`

Read Agent data access and RID version.

`GET /api/research/agent-access` · Reads state

## `research agents revise-access <agent_ref>`

Publish an access revision; existing Teams remain pinned.

`POST /api/research/agents/{agent_ref}/access-revisions` · Writes state

Requires `--json FILE` (or `--json -`), containing a JSON object.

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| JSON only | `rid_version` | str | Required | — |
| JSON only | `data_access` | array | Required | [{product, feed_scope?: {rids: [...]}}] |

## `research agents revise-instructions <agent_ref>`

Publish instructions from a UTF-8 file; existing Teams remain pinned.

`POST /api/research/agents/{agent_ref}/instruction-revisions` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--instructions-file` | `instructions` | string (UTF-8 file with flags) | Required | UTF-8 file, or - for stdin; JSON input uses the instructions string |

## `research teams list`

List Teams and daily enablement.

`GET /api/research/teams` · Reads state

## `research teams create`

Publish a Team with an explicit non-empty Agent selection.

`POST /api/research/teams` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--team-id` | `team_id` | str | Required | — |
| `--title` | `title` | str | Required | — |
| `--scope` | `scope` | str | Required | security or market; must match the selected Team |
| `--agent-id` | `agent_ids` | array | Required / repeat flag | Published Agent ID; repeat for each member |

## `research requests create`

Queue research; HTTP 202 means accepted, not completed.

`POST /api/research/requests` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--team-ref` | `team_ref` | str | Required | — |
| `--scope` | `scope` | str | Required | security or market; must match the selected Team |
| `--code` | `code` | str | Optional | Six-digit stock code; omitted for market scope (sends null) |
| `--submission-identity` | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |

## `research requests current`

Read queued/running requests and worker state.

`GET /api/research/requests/current` · Reads state

## `research requests get <request_id>`

Read request status, including terminal no-report states.

`GET /api/research/requests/{request_id}` · Reads state

## `research requests cancel <request_id>`

Request cancellation of unfinished research.

`POST /api/research/requests/{request_id}/cancel` · Writes state

## `research requests rerun <request_id>`

Queue a new run of a terminal request.

`POST /api/research/requests/{request_id}/rerun` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--submission-identity` | `submission_identity` | str | Required | Caller-chosen stable ID; reuse only for the same submission |

## `research records list`

List research records; default statuses are passed and partial.

`GET /api/research/records` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--team-id` | `team_id` | str | Optional | — |
| `--team-ref` | `team_ref` | str | Optional | — |
| `--status` | `status` | array | Optional / repeat flag | — |
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--offset` | `offset` | int | Optional | Zero-based offset |

## `research records get <record_id>`

Read a record and integrity-checked report when available.

`GET /api/research/records/{record_id}` · Reads state

## `research daily-teams enable <team_ref>`

Enable this exact Team version for daily research.

`PUT /api/research/daily-teams/{team_ref}` · Writes state

## `research daily-teams disable <team_ref>`

Disable this exact Team version for daily research.

`DELETE /api/research/daily-teams/{team_ref}` · Writes state

## `profiles list`

List available stock profiles.

`GET /api/profiles` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |

## `profiles get <code>`

Read a six-digit stock profile.

`GET /api/profiles/{code}` · Reads state

## `charts list`

List available chart assets.

`GET /api/charts` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |

## `charts download <asset_id>`

Save a chart image to a new file.

`GET /api/charts/{asset_id}` · Reads state

Requires `--output PATH`; writes original bytes to a new file and returns path, byte count, SHA-256, and content type. Existing files are never replaced.

## `ledger transactions list`

Read recorded transactions and ledger state.

`GET /api/ledger/transactions` · Reads state

Query fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--limit` | `limit` | int | Optional | Page size; API default and bounds apply |
| `--offset` | `offset` | int | Optional | Zero-based offset |

## `ledger transactions add`

Record an existing transaction; never submits an order.

`POST /api/ledger/transactions` · Writes state

Body fields:

| Option | JSON/wire field | Type | Requirement | Meaning |
|---|---|---|---|---|
| `--transaction-id` | `transaction_id` | str | Required | — |
| `--account-id` | `account_id` | str | Optional | — |
| `--trade-date` | `trade_date` | str | Required | — |
| `--transaction-type` | `transaction_type` | str | Required | cash_deposit, cash_withdrawal, buy, sell, fee, or tax |
| `--code` | `code` | str | Optional | — |
| `--quantity` | `quantity` | int | Required | — |
| `--price` | `price` | float | Required | — |
| `--amount` | `amount` | float | Required | — |
| `--fees` | `fees` | float | Required | — |

## `ledger import`

Import a JSON array of existing transactions (not CSV or orders).

`POST /api/ledger/import` · Writes state

Requires `--json FILE` (or `--json -`), containing a JSON array.
